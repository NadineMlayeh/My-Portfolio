# og.png is referenced but does not exist yet

`index.html` points `og:image` and `twitter:image` at `/og.png`. Until that file is here, links
pasted into LinkedIn, Slack or an application form show a preview with no image.

**What it needs to be:** 1200 × 630 PNG. Name, role, and one or two of the strongest project
visuals. Dark background to match the site.

Quickest route: build it in Figma or Canva at 1200 × 630, export as PNG, save it here as
`og.png`, and delete this note.
