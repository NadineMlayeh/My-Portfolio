# Job Tracker — screenshots

Drop your screen captures here for the "Job Tracker" project card.

Name them `1.png`, `2.png`, `3.png`, `4.png` to match the card in
`src/main.jsx` (add more by editing the `screens` array — order matters).

The card auto-scrolls through the images and pauses while hovered.